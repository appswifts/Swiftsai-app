---
name: "Build Containers"

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-containers-common:
    runs-on: ubuntu-latest
    outputs:
      containerver: ${{ steps.getcontainerver.outputs.containerver }}
    steps:
      - name: Get Container Version
        id: getcontainerver
        run: |
          echo "containerver=latest" >> "$GITHUB_OUTPUT"

  build-containers:
    needs: build-containers-common
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          - runnertags: ubuntu-latest
            arch: amd64
    runs-on: ${{ matrix.runnertags }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and Push Image
        env:
          CONTAINERVER: ${{ needs.build-containers-common.outputs.containerver }}
        run: |
          docker buildx build --platform linux/amd64 \
            -f Dockerfile.dev \
            -t ghcr.io/appswifts/swiftsai-app:latest \
            --pull \
            --no-cache \
            --provenance=false --sbom=false \
            --output "type=registry,name=ghcr.io/appswifts/swiftsai-app:latest" .

  deploy:
    needs: build-containers
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Server
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          command_timeout: 30m
          script_stop: true
          script: |
            set -eu
            cd ~/postiz-docker-compose
            sudo docker compose pull postiz
            sudo docker compose up -d --no-deps --force-recreate postiz

            for attempt in $(seq 1 60); do
              if curl --fail --silent --show-error http://127.0.0.1:4007/ >/dev/null; then
                exit 0
              fi
              sleep 5
            done

            echo "Application did not become healthy within 5 minutes" >&2
            sudo docker compose logs --tail=200 postiz
            exit 1
